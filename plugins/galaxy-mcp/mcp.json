{
  "mcpServers": {
    "galaxy": {
      "command": "uvx",
      "args": ["galaxy-mcp"],
      "env": {
        "GALAXY_URL": "${env:GALAXY_URL}",
        "GALAXY_API_KEY": "${env:GALAXY_API_KEY}"
      }
    }
  }
}
