{
  "mcpServers": {
    "galaxy": {
      "command": "uvx",
      "args": ["galaxy-mcp"],
      "env": {
        "GALAXY_URL": "${user_config.galaxy_url}",
        "GALAXY_API_KEY": "${user_config.galaxy_api_key}"
      }
    }
  }
}
